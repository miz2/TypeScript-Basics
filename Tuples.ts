const user:(string|number)[]=[1,'hc']

const user2:string[]=['hc']
// must be in this order 
let tUser:[string,number,boolean]
tUser=["hc",131,true]
let rgb:[number,number,number]=[244,231,241]

type Use=[number,string]
const newUser:Use=[23,"dskbhkfhbjs"]
// can aslos change 
newUser[1]="hc fjhsf"
// it is basically array with restrictions 
// newUser.push(true); now this has been fixed 

export{}